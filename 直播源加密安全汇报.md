# 网站直播源加密安全汇报

**审计范围：** `app.d2587734.js`、`chunk-vendors.36dd8787.js` 与示例密文 `en.txt`。  
**审计日期：** 2026-09-23。  
**结论摘要：** 当前实现是“客户端可逆混淆”，不是能够保护直播源或接口响应机密性的加密边界。应将其作为兼容期遗留协议下线，并把鉴权、授权和源站访问控制迁移到服务端。

## 1. 发现的协议与实现链路

### 1.1 算法

业务 bundle 的 `42b6` 模块定义了加/解密类：

1. 将明文按 UTF-8 转成 CryptoJS `WordArray`；
2. 调用 CryptoJS `AES.encrypt` / `AES.decrypt`；
3. 显式选择 `mode.ECB` 和 `pad.Pkcs7`；
4. 将密文序列化为 Base64 字符串，解密结果再按 UTF-8 输出。

因此实际格式是：

```text
Base64( AES-128-ECB( PKCS#7(UTF-8 明文), 固定 128-bit key ) )
```

`chunk-vendors.36dd8787.js` 提供 CryptoJS AES、ECB 模式和 PKCS#7 实现；业务 bundle 调用该库完成上述协议。两份文件中的“加密”代码不是 DRM，也没有与用户、会话、设备或播放时效绑定。

### 1.2 密钥还原

密钥没有明文常量，而是由四组 4 字节数组与 `[0x12, 0x34, 0x56, 0x78]` 中同组掩码逐字节 XOR 得出。这只增加阅读成本，不增加密钥保密性：浏览器下载 bundle 后即可重复同一计算。

还原后的 16 字节密钥为：

```text
KVksL2jJ6eLOP7cX
```

> 本报告将该值视为**已暴露的遗留密钥**。不要把它迁移到任何新协议、代码库、环境变量或生产密钥管理系统。

### 1.3 与直播功能的关系

bundle 中存在 `app/encryptionHostLive`、`app/encryptionFocusHost`、`app/encryptionLiveInfo` 等路由/接口命名，且 HTTP 客户端封装会在请求参数中加密、在 200 响应后解密并尝试 JSON 解析。这说明机制主要在应用层包装接口载荷；它没有阻止获得浏览器脚本和正常会话的用户提取解密后的直播地址或响应。

## 2. `en.txt` 验证结果

当前 `en.txt` 解码后仍满足 AES 块大小要求。使用上面的 AES-128-ECB/PKCS#7 协议及还原后的密钥解密成功，UTF-8 明文为一个 JSON 响应：

- `code` 为 `"200"`，`msg` 为 `"成功"`；
- `data` 为赛事记录数组；
- 完整的、格式化后的示例明文已保存为仓库根目录的 `decrypted-example.json`，便于在受控审计或迁移验证中查看。

该示例包含赛事名称、比分和公开图片 URL 等接口响应字段。它不应被视为直播源保护方案的一部分，也不应将其中内容或遗留密钥部署至前端或公开运维接口。

## 3. 安全评估

| 风险 | 严重性 | 依据与影响 |
| --- | --- | --- |
| 固定密钥随前端发放 | 高 | 所有访客可取得相同 key；重新实现解密不需要攻破 AES。 |
| ECB 模式不提供语义安全 | 高 | 相同明文块在相同 key 下产生相同密文块，泄露结构与重复字段模式；也不适合保护结构化 JSON。 |
| 缺少完整性/真实性校验 | 高 | AES-ECB + PKCS#7 没有认证标签，不能检测密文篡改或可靠证明来源。 |
| 前端混淆被误作访问控制 | 高 | JavaScript、算法与 key 必须交给客户端，无法用于阻止获得页面的用户读取明文。 |
| 业务响应解密后直接 JSON 解析 | 中 | 完整性失败和格式异常只能通过解析失败被动发现，无法区分篡改、过期、传输错误。 |
| 缺少绑定与过期语义 | 高（若直播 URL 长期有效） | 密文格式本身未绑定用户、会话、设备、频道、nonce 或过期时间；泄露后难以限制重放。 |

**重要区分：** TLS 保护传输链路；这里的 AES 只包装了到达客户端后仍必须被客户端解开的数据。两者不能互相替代。

## 4. 对直播源保护的影响

1. 攻击者不必破解 AES：只需从公开 bundle 中恢复 key，并用标准库 AES-ECB 解密。
2. 即使替换为更强的“前端对称加密”，只要 key 仍在浏览器，授权用户仍可在解密点、网络调试工具或运行时内存中得到播放地址。
3. 若源站接受长期静态 URL，URL 一旦在客户端出现就可能被复制、转发和重放；加密字符串本身并不能撤销该能力。
4. 仅靠前端禁用 DevTools、代码压缩或混淆不能构成直播内容保护措施。

## 5. 建议整改方案

### P0：立即处置

- 将当前固定 key 视为泄露：停止用它保护任何新增内容；清查其使用范围。
- 确认播放域名/CDN 不允许匿名回源；删除或轮换任何长期、可复用的播放 URL。
- 在网关/CDN 记录签发、播放、拒绝与异常重放事件，并对异常并发、地域和 UA 做告警。
- 保留本仓库中的解密脚本仅用于受控审计/回归验证；不要部署到网站前端或公开运维接口。

### P1：正确的访问控制

- 由服务端在完成登录、会员资格、地域、内容许可和风控校验后，签发**短时**播放令牌或 CDN signed URL/cookie。
- 令牌至少包含频道/资源、主体或会话、过期时间、签发时间、唯一 ID，并由服务端签名；服务端/CDN 必须验证签名与过期。
- 令牌只传给已授权会话；全站启用 HTTPS、HSTS、合适的 CORS、Referrer-Policy 与 Cookie 的 `Secure`、`HttpOnly`、`SameSite` 属性。
- 限制令牌 TTL，支持吊销；不要把真实源站地址或长效上游凭据返回给浏览器。

### P2：内容保护与协议升级

- 若业务需要抵抗“合法用户复制直播内容”，评估 HLS/DASH + DRM（例如 Widevine/FairPlay/PlayReady）及与许可证服务器的授权流程；这比前端 AES 更接近内容保护目标，但仍需配合业务风控与水印。
- 若确有客户端到客户端的机密载荷需求，使用 AEAD（AES-GCM 或 ChaCha20-Poly1305），每条消息生成唯一随机 nonce，并由服务端管理/派生密钥；同时将版本、nonce 和认证标签纳入协议。
- 制定密钥轮换、最小权限、审计日志与秘密管理制度；禁止将生产秘密编译进静态 JS。

### P3：下线和验证

1. 新接口先返回短时服务端签名令牌；保留旧协议只读兼容窗口。
2. 统计旧协议调用并设定下线日期；窗口结束后服务端拒绝旧格式。
3. 在预发布环境验证：过期、跨账号、跨频道、重放、篡改、注销后播放和 CDN 回源均被拒绝。
4. 进行独立渗透测试，并用监控指标验证 URL/令牌泄露后的影响已被 TTL、绑定和吊销控制。

## 6. 审计辅助解密函数

仓库根目录的 `decrypt-sample.js` 用 Node.js 内置 `node:crypto` 重现该**遗留**协议。其输入是 Base64 密文，输出 UTF-8 明文；默认读取 `en.txt`：

```bash
node decrypt-sample.js en.txt
```

该函数包含 Base64 与块长检查，并依赖 OpenSSL 的 PKCS#7 校验。它不应作为生产加密方案，也不应以此 key 加密任何新的直播数据。

## 7. 管理层结论

当前方案可以避免接口响应“直接可读”，但不能保护直播源机密性，也不能作为授权、反盗链、抗重放或 DRM 方案。整改的核心不是继续隐藏前端 key，而是把“是否允许播放”的判定与签名放到服务端/CDN，并让令牌短时、可验证、可撤销；对高价值内容再采用 DRM 与水印等分层措施。
